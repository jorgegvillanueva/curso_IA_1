// Punto único de resolución de dependencias (wiring de adapters).
// Tarea 3: se instanciará aquí el adapter de persistencia elegido.
//
// Ejemplo futuro:
// import { createLocalStorageAdapter } from '../shared/services/persistence/localStorage.adapter';
// export const services = {
//   persistence: createLocalStorageAdapter(),
// };

export const services = {} as const;
