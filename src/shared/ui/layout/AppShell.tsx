import type { ReactNode } from 'react';

interface AppShellProps {
  children: ReactNode;
}

// Placeholder — se implementa en Tarea 5.
export function AppShell({ children }: AppShellProps) {
  return <>{children}</>;
}
